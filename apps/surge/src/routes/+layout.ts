// SvelteKit route configuration for Surge
// Documentation: https://kit.svelte.dev/docs/page-options

// Generate static HTML file for deployment
export const prerender = true;

// Disable SSR for client-side only rendering
export const ssr = false;

// URL trailing slash handling
export const trailingSlash = 'ignore';
